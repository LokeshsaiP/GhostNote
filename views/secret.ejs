<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Your Secret</title>
  </head>
  <body class="bg-[#323232] text-[#ddd0c8] min-h-screen flex flex-col">
    <!-- Navbar -->
    <header class="bg-[#ddd0c8] text-[#323232]">
      <nav
        class="container mx-auto px-6 py-4 flex justify-between items-center"
      >
        <a href="/" class="text-xl font-bold">GhostNote</a>
        <a href="/" class="font-medium hover:text-[#1f1f1f]">Home</a>
      </nav>
    </header>

    <!-- Main Content -->
    <main
      class="flex-grow container mx-auto px-6 py-12 flex items-center justify-center"
    >
      <div
        class="bg-[#3d3d3d] p-8 rounded-xl shadow-lg max-w-lg w-full text-center"
      >
        <% if (revealed) { %>
        <!-- Secret Revealed -->
        <h1 class="text-2xl font-bold mb-4">Your Secret</h1>
        <div
          class="bg-[#2b2b2b] p-6 rounded-md mb-6 break-words text-left leading-relaxed"
        >
          <%= secret %>
        </div>

        <% } else if (hasPassphrase) { %>
        <!-- Secret requires passphrase -->
        <h1 class="text-2xl font-bold mb-4">
          Enter Passphrase to Reveal Secret
        </h1>
        <% if (error) { %>
        <p class="text-red-400 mb-4"><%= error %></p>
        <% } %>
        <form
          method="POST"
          action="/secret/<%= id %>/reveal"
          class="flex flex-col space-y-4"
        >
          <input
            type="password"
            name="passphrase"
            placeholder="Enter Passphrase"
            class="p-4 rounded-md bg-[#2b2b2b] border border-[#555] text-[#ddd0c8] focus:outline-none focus:ring-2 focus:ring-[#ddd0c8]"
          />
          <button
            type="submit"
            class="bg-[#ddd0c8] text-[#323232] font-semibold px-6 py-2 rounded-md hover:bg-[#c9bdb3] transition-all duration-200"
          >
            Reveal Secret
          </button>
        </form>

        <% } else { %>
        <!-- No passphrase, reveal button -->
        <h1 class="text-2xl font-bold mb-4">
          Click below to reveal your secret
        </h1>
        <form method="POST" action="/secret/<%= id %>/reveal">
          <button
            type="submit"
            class="bg-[#ddd0c8] text-[#323232] font-semibold px-6 py-2 rounded-md hover:bg-[#c9bdb3] transition-all duration-200"
          >
            Reveal Secret
          </button>
        </form>
        <% } %>

        <!-- Back Home -->
        <a
          href="/"
          class="inline-block mt-6 bg-[#ddd0c8] text-[#323232] font-semibold px-6 py-2 rounded-md hover:bg-[#c9bdb3] transition-all duration-200"
        >
          ← Back Home
        </a>
      </div>
    </main>
  </body>
</html>
